# Use the official PHP image with Apache
FROM php:7.4-apache

# Copy the current directory (containing index.php) to the Apache document root
COPY . /var/www/html/

# Expose port 80 to the outside world
EXPOSE 80
